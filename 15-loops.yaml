- name: loops
  hosts: localhost
  connection: local
  become: yes

  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "Hello {{ item }}"
      loop:
        - ramesh
        - suresh
        - john
        - bob
        - alice