- name: checking preference for variables
  hosts: localhost
  connection: local
  become: yes
  vars:
    NAME: from play level
  # vars_prompt:
  #   - name: NAME
  #     prompt: what is your input?
  #     private: false
  # vars_files:
  #   - course.yaml

  tasks:
  - name: checking var preference level
    # vars:
    #   NAME: from task level
    ansible.builtin.debug:
      msg: "{{ NAME }}"  

 # 1. cli
 # 2. task
 # 3. files
 # 4. prompt
 # 5.play level
 # 6. inventory     