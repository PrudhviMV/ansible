- name: functions
  hosts: localhost
  connection: local
  become: yes
  vars:
    technologies: DevOps, linux, ansible, shell
    tags:
      Application: payment
      Environment: dev

  tasks:
  - name: functions learn
    ansible.builtin.debug:
      msg: "Hello, {{ name | default('Ramesh') }}"

  tasks:
  - name: split string into list
    ansible.builtin.debug:
      msg: "Technologies are: {{ technologies | split(',') }}"  

  tasks:
  - name: convert dcit2 list
    ansible.builtin.debug:
      msg: "{{ tags|dict2items }}"